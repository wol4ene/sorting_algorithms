#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>

#define TRUE 1
#define FALSE 0

/**
 * Swap two integers
 *
 * @param p1 (i) ptr to first integer
 * @param p2 (i) ptr to second integer
 * @return void
 */
void swap(int *p1, int *p2);

/**
 * Dump an integer array
 *
 * @param arr (i) integer array
 * @param size (i) size of arr
 * @return void
 */
void dumpArray(int *arr, int size);

